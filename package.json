{
  "name": "status-server",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Status API for Cloudflare Workers — receives push status from devices/servers and polls websites/endpoints via cron",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:up": "wrangler d1 migrations apply status-db",
    "db:local": "wrangler d1 migrations apply status-db --local"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
