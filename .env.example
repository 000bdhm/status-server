# Copy to .env and fill in
PORT=3000
# Admin key used for all management & read endpoints (X-Admin-Key header).
# If omitted, a random key is generated and printed on boot.
ADMIN_KEY=change-me-to-a-long-random-string
# Directory for the SQLite database file. Defaults to ./data
DB_PATH=./data
